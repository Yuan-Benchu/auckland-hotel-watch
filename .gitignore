key.txt
__pycache__/

# ---- 本地采集 (local/) ----
# 页面存档: 1100+ 个文件 / 17MB 且持续增长, 是复核证据但不适合进 git
local/data/raw_archive/
# 运行时产物与日志
local/data/*.log
local/data/alert_state.json
local/data/overnight_report.md
# 采集进程每 20 秒往这里追加一次, 跟踪它会让工作区永远是脏的,
# 每次 git 操作都被挡。它是原始追加日志, 真正要保留的是 curate.py
# 产出的 hotel_prices_curated.csv(已跟踪), 需要时重新生成并提交快照。
local/data/hotel_prices_v3.csv
# 已作废的原始数据(解析器有 bug, 见 local/data/CLEANING.md), 只保留 curated
local/data/hotel_prices_v2.csv
local/data/hotel_prices_fast.csv
local/data/hotel_prices.csv
local/data/hotel_prices_merged.csv
local/data/hotel_prices_clean.csv
# 看板产物(可由模板+数据重新生成)
local/dashboards/_built.html
local/dashboards/auckland_*.html
# 凭据与备份
local/dida_key.txt
local/*.bak
local/*.bak2
local/*.bak3

# 某个工具自动建的 worktree 副本, 不是项目内容
.kilo/

# 每次重建看板都会重新生成, 跟踪它们会让工作区永远是脏的,
# 挡住 rebase / filter-branch。模板和数据都在库里, 随时能重建。
local/data/dashboard_data.json
local/data/timeline_data.json
local/data/.cycle_done
local/dashboards/dashboard_data.json
local/dashboards/timeline_data.json

# 看门狗运行日志与 PID 文件, 属于运行时状态, 不入库
local/data/watchdog.log
local/data/runner.pid
